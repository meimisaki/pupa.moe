'use strict';

var uuid = require('../../uuid');

function genToken(id, callback) {
	var client = this._;
	var token = uuid();
	client.setex('token:' + id, 86400, token, function (err) {
		callback(err, token);
	});
}

module.exports = genToken;
